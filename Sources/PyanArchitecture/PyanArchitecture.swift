//
//  PyanArchitecture.swift
//  PyanArchitecture
//
//  Created by Perceval Archimbaud on 05/02/2026.
//

/// Re-exports PyanInject so consumers only need to `import PyanArchitecture`.
@_exported import PyanInject
/// Re-exports PyanRouter so consumers only need to `import PyanArchitecture`.
@_exported import PyanRouter
/// Re-exports PyanLogging so consumers only need to `import PyanArchitecture`.
@_exported import PyanLogging
/// Re-exports PyanFeatureSwitcher so consumers only need to `import PyanArchitecture`.
@_exported import PyanFeatureSwitcher
/// Re-exports PyanMocking so consumers only need to `import PyanArchitecture`.
@_exported import PyanMocking
